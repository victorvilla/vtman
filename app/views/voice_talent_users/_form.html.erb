<%= form_for(@voice_talent_user) do |f| %>
  <% if @voice_talent_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@voice_talent_user.errors.count, "error") %> prohibited this voice_talent_user from being saved:</h2>

      <ul>
      <% @voice_talent_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
